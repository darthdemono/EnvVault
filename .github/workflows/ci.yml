name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege: nothing here writes to the repo. Build & Release asks for
# more, in the one job that needs it.
permissions:
  contents: read

concurrency:
  # A superseded push has nothing to tell us; cancel it and free the runner.
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

# Warnings are denied via `[workspace.lints.rust]` in the root Cargo.toml, not
# via RUSTFLAGS here. RUSTFLAGS would apply to dependencies too, so a warning in
# a platform-specific crate like `windows-sys` or `openssl-src` would fail this
# job with nothing in the repository to fix. It also means `cargo check` locally
# enforces exactly what CI enforces.
#
# Consequence: with no RUSTFLAGS set, `.cargo/config.toml` is no longer
# overridden, so its `-fuse-ld=mold` applies on Linux and mold has to be
# installed below — the same thing Build & Release already does.

jobs:
  rust:
    name: Rust (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      # One platform failing should still report the other, or a Windows-only
      # break hides behind a green Linux run and vice versa.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      # rusqlite's `sqlcipher` feature links against the system SQLCipher, and
      # the Tauri crate needs the WebKitGTK stack even for `cargo check`. mold is
      # here because `.cargo/config.toml` passes `-fuse-ld=mold` for this target.
      # Windows needs none of this: it vendors SQLCipher and OpenSSL (see
      # vault-core/Cargo.toml) and WebView2 ships with the runner image.
      - name: Install system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libsqlcipher-dev \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            mold

      # Windows has no system SQLCipher, so vault-core's cfg(windows) dependency
      # vendors SQLCipher *and* OpenSSL — and building OpenSSL from source needs
      # a real Perl and NASM. Failing here, naming the cause, beats an opaque
      # openssl-src error a thousand lines into a build log.
      # openssl-src builds OpenSSL from source, and OpenSSL's Configure is a Perl
      # program that needs a full Perl — Locale::Maketext::Simple among others.
      #
      # windows-latest ships Strawberry Perl, which has those modules. It also
      # ships Git for Windows, whose bundled MSYS Perl does NOT — and the build
      # steps below run under `shell: bash`, where Git's `/usr/bin/perl` comes
      # first on PATH. So `perl` resolves, `perl --version` prints happily, and
      # the build dies a thousand lines later with:
      #
      #   Can't locate Locale/Maketext/Simple.pm in @INC
      #
      # Putting Strawberry ahead of it on PATH is the whole fix. `C:\Strawberry\c\bin`
      # is included because Configure also wants the toolchain that ships with it.
      - name: Put Strawberry Perl ahead of Git's Perl on PATH
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          if (-not (Test-Path 'C:\Strawberry\perl\bin\perl.exe')) {
            choco install strawberryperl -y --no-progress
          }
          'C:\Strawberry\c\bin', 'C:\Strawberry\perl\site\bin', 'C:\Strawberry\perl\bin' |
            ForEach-Object { Add-Content -Path $env:GITHUB_PATH -Value $_ }

      # Assert the toolchain can actually do the job, not merely that the
      # binaries exist. `command -v perl` passed for the whole life of the
      # earlier version of this check while the Perl it found was the one that
      # cannot build OpenSSL — presence was never the property that mattered.
      - name: Verify the vendored-OpenSSL toolchain
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          missing=""
          command -v perl >/dev/null || missing="$missing perl"
          command -v nasm >/dev/null || missing="$missing nasm"
          if [ -n "$missing" ]; then
            echo "::error::openssl-src needs$missing on PATH to vendor OpenSSL for Windows."
            exit 1
          fi
          echo "perl: $(command -v perl)"
          perl --version | sed -n '2p'
          nasm -v
          if ! perl -MLocale::Maketext::Simple -e 1 2>/dev/null; then
            echo "::error::The perl on PATH cannot load Locale::Maketext::Simple, so OpenSSL's Configure will fail. This is usually Git for Windows' MSYS perl shadowing Strawberry Perl."
            exit 1
          fi
          echo "Locale::Maketext::Simple loads — OpenSSL's Configure will run."

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - uses: Swatinem/rust-cache@v2

      # `cargo check`/`cargo test` reach the Tauri app crate, whose build script
      # fails outright when `frontendDist` ("../dist") is missing — and dist/ is
      # gitignored. Build it here so the Rust jobs have something to point at.
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Build the frontend (tauri-build needs ../dist to exist)
        run: |
          npm ci
          npx vite build

      # `--locked` below refuses to build if a Cargo.toml edit was committed
      # without the matching Cargo.lock. Saying so here beats a resolver error
      # buried in the test job's output.
      - name: Cargo.lock is up to date
        run: cargo metadata --locked --format-version 1

      # vault-core holds the crypto, storage and RBAC logic; envv-server holds the
      # session and LAN-hosting logic. Regressions in either are security bugs
      # rather than cosmetic ones.
      - name: Test
        run: cargo test --workspace --locked --verbose

      - name: Check the whole workspace
        run: cargo check --workspace --locked --all-targets

      # Formatting is platform-independent, so checking it on both runners would
      # pay twice for the same answer. `--check` prints a diff and exits
      # non-zero; it never rewrites the tree.
      - name: Formatting (rustfmt)
        if: runner.os == 'Linux'
        run: cargo fmt --all -- --check

      # Clippy runs on both, because the lints that matter here are behind
      # `cfg` — vault-core vendors SQLCipher and OpenSSL only on Windows, and
      # envv-cli's process-spawning code takes a different branch there. A
      # Linux-only clippy would never see either.
      #
      # No `-D warnings` on the command line: the levels live in the root
      # Cargo.toml under `[workspace.lints.clippy]`, so `cargo clippy` locally
      # enforces exactly what this line does. Passing the flag here as well
      # would apply it to dependencies too.
      - name: Clippy
        run: cargo clippy --workspace --locked --all-targets

      # The portable build path — SQLCipher and OpenSSL compiled in — is what
      # release artefacts use, so it has to be exercised somewhere. Checking it
      # on Linux catches a broken feature union without paying for it on both.
      - name: Check the bundled (distro-independent) build
        if: runner.os == 'Linux'
        run: cargo check -p vault-core --locked --features bundled

  frontend:
    name: Frontend (typecheck + test + build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # First, because it is the cheapest and the one whose failure invalidates
      # a release rather than a commit. The version is stated in six files and
      # nothing else in this workflow reads more than one of them.
      - name: Version numbers agree
        run: npm run version:check

      # `--check` only reports. Reformatting in CI and committing the result
      # would mean the tree on disk never matches the tree that was reviewed.
      - name: Formatting (Prettier)
        run: npm run format:check

      # `eslint-suppressions.json` records the 237 pre-existing violations this
      # config found when it was introduced, so this step passes today and
      # fails on anything NEW. Fix some of the backlog and run
      # `npx eslint --prune-suppressions` to shrink the file — never regenerate
      # it wholesale with `--suppress-all`, which would silently absorb real
      # regressions along with the old debt.
      - name: Lint (ESLint)
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: UI tests
        run: npm test

      - name: Build
        run: npx vite build
