# Build output and dependencies
dist/
coverage/
target/
node_modules/
src-tauri/gen/

# Generated API references, and the Pages site assembled from them.
docs/
site/

# Lockfiles are generated; reformatting them creates diff noise and can make a
# tool that reads them byte-wise unhappy.
package-lock.json
Cargo.lock

# Golden fixtures. These files ARE the assertion — tests/cli-parity.test.ts and
# envv-cli/tests/parity.rs both compare exporter output against these exact
# bytes. Reformatting one makes both sides fail against a file that no longer
# describes any real config format.
tests/fixtures/

# index.html is formatted by hand. A formatter has already silently deleted an
# element from it once (#new-category-form, Phase 3), and the app queries ids
# that no tool can prove are still present. See CLAUDE.md, Phase 3 pitfalls.
index.html

# Session logs and LLM scratch documents; gitignored, so formatting them only
# produces local churn.
CLAUDE.md
CLAUDE.original.md
Handoff-*.md
*.original.md
repomix-output.md
.claude/

# Generated by `eslint --suppress-all` / `--prune-suppressions`. Formatting it
# would make every regeneration show up as an unformatted file, and CI's
# format:check would fail on a file no human edits.
eslint-suppressions.json
