# Generated by EnvVault
- hosts: all
  vars:
    # db
    db_user: "appuser"
    db_password: "pg-secret-value"
  tasks:
    - name: Restart app
      ansible.builtin.service:
        state: "restarted"