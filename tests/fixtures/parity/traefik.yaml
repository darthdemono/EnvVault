# Generated by EnvVault
http:
  routers:
    web:
      rule: "Host(`app.example.com`)"
      service: "app-svc"
      entryPoints:
        - web
        - websecure
  services:
    app-svc:
      loadBalancer:
        passHostHeader: true
        servers:
          - url: "http://127.0.0.1:8080"
  middlewares:
    auth:
      basicAuth:
        users: admin:$apr1$abc$def